$$
\mathrm{TransverseStiffness} \leftarrow (\mathrm{TransverseStiffness}+\mathrm{VelStiffness})\times(1-|\mathrm{PoseBoneDir}\cdot \mathrm{BoneDir}|)^2
$$


$$
\mathrm{TransverseStiffness} \leftarrow (\mathrm{TransverseStiffness}+\mathrm{VelStiffness})\times\left[ 1-\frac{(\mathrm{PoseBoneVec}\cdot \mathrm{BoneVec})^2}{\mathrm{PoseBoneVec}^2\times \mathrm{\mathrm{BoneVec}}^2} \right]^2
$$


$$
\begin{aligned}
qx &= (c_{x}, s_{x}, 0,0)\\
qy &= (c_{y},0,s_{y},0)\\
qz &= (c_{z}, 0,0,s_{z})\\
q &= q_{y}\cdot q_{x}\cdot q_{z}
\end{aligned}
$$

$$
\begin{aligned}
[][s_a,\vec{a}][s_b,\vec{b}] = [&s_as_b-x_ax_b-y_ay_b-z_az_b,\\
&s_a(x_b\vec{i}+y_b\vec{j}+z_b\vec{k})+s_b(x_a\vec{i}+x_a\vec{j}+x_a\vec{k})\\
&+(y_az_b-y_bz_a)\vec{i}+(z_ax_b-z_bx_a)\vec{j}+(x_ay_b-x_by_a)\vec{k}]
\end{aligned}
$$

$$
[s_a,\vec{a}][s_b,\vec{b}] = [s_as_b-\vec{a}\cdot\vec{b},s_a\vec{b}+s_b\vec{a}+\vec{a}\times \vec{b}]
$$

$$
q_{x}q_{z} = (c_{x}c_{z}, c_{z}s_{x}, -s_{x}s_{z}, c_{x}s_{z})
$$


$$
\begin{aligned}
q_{y}(q_{x}q_{z}) &= (c_{x}c_{y}c_{z}+s_{x}s_{y}s_{z},  s_{x}c_{y}c_{z}, -s_{x}c_{y}s_{z},c_{x}c_{y}s_{z}  )\\
&+(0, 0, c_{x}s_{y}c_{z}, 0) \\
&+(c_{x}s_{y}s_{z}, 0, -s_{x}s_{y}c_{z})\\
&= (c_{x}c_{y}c_{z}+s_{x}s_{y}s_{z},  \\
& s_{x}c_{y}c_{z}+c_{x}s_{y}s_{z},\\
&c_{x}s_{y}c_{z}-s_{x}c_{y}s_{z},\\
&c_{x}c_{y}s_{z}-s_{x}s_{y}c_{z}  )
\end{aligned}
$$


$$
\begin{aligned}
f(u) &= C'(u)\cdot(C(u)-P),\\
u_{i+1}&=u_{i}- \frac{f(u_{i})}{f'(u_{i})}=u_{i} -  \frac{C'(u_{i})\cdot(C(u_{i})-P)}{C''(u_{i})\cdot(C(u_{i})-P)+|C'(u_{i})|^2}
\end{aligned}
$$


$$
f(u) = \frac{Dir\cdot(C(u)-P)}{|C(u)-P|} - 1 \implies g(u) = [Dir\cdot(C(u)-P)]^2 - (C(u)-P)^2
$$


$$
\begin{aligned}
\mathrm{Scale} &= \max(\mathrm{RightRotAngle},\mathrm{LeftRotAngle})^2 \\
\mathrm{ActualOffset} &= \mathrm{Scale}\times \mathrm{Offset_{LSPIA}}
\end{aligned}
$$



设 $A,B,C$ 是三角形的顶点，$P$ 是骨骼点。若 $A,B,C,P$ 共面，则需要计算重心坐标 $(1-u-v,u,v)$ 使得
$$
(1-u-v)A +uB+vC = P
$$
整理得到
$$
(u,v,1)
\begin{pmatrix}
AB_{x} & AB_y & AB_{z} \\
AC_{x} & AC_{y} & AC_{z} \\
PA_{x} & PA_{y} & PA_{z}
\end{pmatrix}
= 0
$$
因此 $(u,v,1)$ 与任意两列向量的叉积共线，因此可以任取两列向量做叉积，然后对 $z$ 坐标归一化得到重心坐标。


不使用叉积的计算方法：用边 $AB,AC$ 作为基向量分解 $AP$ 得到
$$
AP = \alpha AB+\beta AC \implies
\begin{pmatrix}
AP\cdot AB \\
AP\cdot AC
\end{pmatrix} = 
\begin{pmatrix}
AB\cdot AB & AB\cdot AC \\
AC\cdot AB & AC\cdot AC
\end{pmatrix}
\begin{pmatrix}
\alpha \\
\beta
\end{pmatrix}
$$
求解二阶矩阵方程得到 $\alpha,\beta$，对应的重心坐标为 $(1-\alpha-\beta,\alpha,\beta)$ 。



我们希望找到一个最佳的


$$
F(T,R) = \|R(L(0)-L(t))+L'(t)-L(0) \|^2+\|R(L(1)-L(t))+L'(t)-L(1) \|^2
$$



考虑优化目标
$$
F(t)=\sum_{i=1}^n\|Rp_{i}+t-q_{i}\|^2,\quad p_{i}\in A,q_{i}\in B
$$
令导数为零得到
$$
t = \frac{1}{n}\sum_{i=1}^nq_{i}-R\frac{1}{n}\sum_{i=1}^np_{i} = \bar{q}-R\bar{p}
$$
代回得到


可以将优化目标重写为
$$
\begin{aligned}
\mathrm{arg}&\min_{R}\sum_{i=1}^2\|Rx_{i}-y_{i}\|^2,\\
x_{1}&=L(0)-L(t),y_{1}=L(0)-L'(t),\\
x_{2}&=L(1)-L(t),y_{2}=L(1)-L'(t)
\end{aligned}
$$
展开表达式得到
$$
\mathrm{arg}\min_{R}\sum_{i=1}^2x_{i}^Tx_{i}-2y_{i}^TRx_{i}+y_{i}^Ty_{i} = \mathrm{arg}\max_{R}\sum_{i=1}^2y_{i}^TRx_{i}
$$
将其写成 $3\times 2$ 矩阵的形式
$$
\sum_{i=1}^2y_{i}^TRx_{i} = \mathrm{tr}(Y^TRX) = \mathrm{tr}(RXY^T) = \mathrm{tr}(RS)
$$
其中利用了 $\mathrm{tr}(AB)=\mathrm{tr}(BA)$ 。于是
$$
\mathrm{tr}(RS)= \mathrm{tr}(RU\Sigma V^T)=\mathrm{tr}(\Sigma V^TRU) = \mathrm{tr}(\Sigma M)
$$
显然 $M=V^TRU$ 是正交阵，其列向量都是单位向量，因此 $|m_{ij}|\leq 1$，则有
$$
|\mathrm{tr}(\Sigma M)|=\left|\sum_{i=1}^3\sigma_{i}m_{ii}\right| \leq \sum_{i=1}^3\sigma_{i}
$$
在 $|m_{ii}|=1$ 时取得最大值，此时必有
$$
M=\begin{pmatrix}
1 \\
 & 1 \\
 &  & 1
\end{pmatrix}\quad \mathrm{or}\quad M=\begin{pmatrix}
1 \\
 & 1 \\
 &  & -1
\end{pmatrix}
$$
因此
$$
R = V
\begin{pmatrix}
1 \\
 & 1 \\
 &  & \det(VU^T)
\end{pmatrix}
U^T
$$
就是目标旋转。


$$
S=U\Sigma V^T
$$

首先，注意到 $x_{1},x_{2}$ 共线，因此
$$
y_{1}^TRx_{1}+y_{2}^TRx_{2}=(y_{1}^T+ky_{2}^T)Rx_{1}=y^TRx=Rxy^T=RS
$$
根据奇异值分解形式，我们有
$$
(x^Tx)yy^T=(xy^T)^Txy^T=V\Sigma^2V^T,\quad (y^Ty)xx^T=xy^T(xy^T)^T=U\Sigma^2U^T
$$
因此有
$$
yy^T= \frac{1}{x^Tx}V\Sigma^2V^T,\quad  x x^T=\frac{1}{y^Ty}U\Sigma^2U^T
$$
由于 $xx^T,yy^T$ 都是秩一对称阵，因此它们合同/相似于对角阵。并且
$$
(x x^T)x = (x^Tx)x
$$
因此 $x x^T$ 有唯一非零特征值 $x^Tx$ 和特征向量 $x$ 。于是可以直接给出分解式
$$
\begin{aligned}
x x^T &= 
\begin{pmatrix}
\hat{x} & n & \hat{x}\times n
\end{pmatrix}
\begin{pmatrix}
x^Tx \\
 & 0 \\
 &  & 0
\end{pmatrix}
\begin{pmatrix}
\hat{x}^T \\
n^T \\
(\hat{x}\times n)^T
\end{pmatrix},\\
yy^T &= 
\begin{pmatrix}
\hat{y} & n & \hat{y}\times n
\end{pmatrix}
\begin{pmatrix}
y^Ty \\
 & 0 \\
 &  & 0
\end{pmatrix}
\begin{pmatrix}
\hat{y}^T \\
n^T \\
(\hat{y}\times n)^T
\end{pmatrix}\\
n &= \hat{x} \times \hat{y}
\end{aligned}
$$
于是
$$
\begin{aligned}
U &= \begin{pmatrix}
\hat{x} & n & \hat{x}\times n
\end{pmatrix}\\
V &= \begin{pmatrix}
\hat{y} & n & \hat{y}\times n
\end{pmatrix}
\end{aligned}
$$
代回得到
$$
\begin{aligned}
R &= V
\begin{pmatrix}
1 \\
 & 1 \\
 &  & \det(VU^T)
\end{pmatrix}
U^T \\
&= 
\begin{pmatrix}
\hat{x} & n & \hat{x}\times n
\end{pmatrix}
\begin{pmatrix}
1 \\
 & 1 \\
 &  & \det(VU^T)
\end{pmatrix}
\begin{pmatrix}
\hat{y}^T \\
n^T \\
(\hat{y}\times n)^T
\end{pmatrix}\\
&=
\hat{x}\hat{y}^T+nn^T+\det(VU^T)(\hat{x} \times n)(\hat{y}\times n)^T
\end{aligned}
$$
于是
$$
Rn = \hat{x}\hat{y}^Tn+nn^Tn+\det(VU^T)(\hat{x} \times n)(\hat{y}\times n)^Tn = n
$$
因此 $n$ 是 $R$ 的特征向量，$R$ 是绕 $n$ 的转动，且由于 $n$ 与 $x,y$ 正交，即证。



$$
\begin{aligned}
S(\theta) &= (d-ra\sin \theta)^2+[ra(1-\cos \theta)]^2 + (d+a\sin \theta)^2+[a(1-\cos \theta)]^2\\
&= 2a[a(r^2+1)(1-\cos \theta)-(r-1)d\sin \theta]\\
S'(\theta) &= 2a[a(r^2+1)\sin \theta-(r-1)d\cos \theta]\\
S''(\theta) &= 2a[a(r^2+1)\cos \theta+(r-1)d\sin \theta] \geq 0
\end{aligned}
$$
因此 $S(\theta)$ 是凸函数，则 $S'(\theta)=0$ 对应最小值点，解得
$$
\tan \theta = \frac{(r-1)d}{a(r^2+1)}
$$
设线段总长为 $L$，则 $(1+r)a=L$，代入得到
$$
\theta = \arctan\left( \frac{d}{L} \frac{r^2-1}{r^2+1} \right) \geq 0
$$
当 $r=1$ 时 $\theta=0$；且当 $r>1$ 时，$\theta>0$，与我们的期望一致。

